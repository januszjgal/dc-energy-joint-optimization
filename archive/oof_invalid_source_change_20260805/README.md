# Invalidated OOF Wave

The first 16-process OOF training wave was intentionally stopped after an
active evaluation source file changed following protocol freeze.

The campaign's provenance guard would have rejected every model before atomic
publication. Partial temporary models, logs, and the obsolete protocol snapshot
are retained here only to document that invalidation. They are not resumable
and must not be used for results.
