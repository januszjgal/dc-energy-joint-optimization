\documentclass[conference]{IEEEtran}

\usepackage{newtxtext,newtxmath}
\usepackage{amsmath}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{array}
\usepackage{url}
\usepackage[hidelinks]{hyperref}

\urlstyle{tt}
\setlength{\emergencystretch}{3em}
\newcommand{\codepath}[1]{{\Urlmuskip=0mu plus 1mu\relax\nolinkurl{#1}}}
\graphicspath{{../docs/figures/ppo_pilot/}}

\begin{document}

\title{A Proximal Policy Optimization Scheduler for Joint Ramp and Peak
Reduction: Method and Three-Seed Pilot (Draft)}

\author{\IEEEauthorblockN{Janusz Gal}
\IEEEauthorblockA{jgal@charlotte.edu}}

\maketitle

\begin{IEEEkeywords}
proximal policy optimization, reinforcement learning, data centers, grid
ramp, net-load peak
\end{IEEEkeywords}

\section{Introduction}
\label{sec:introduction}

The problem-statement chapter defines a scheduler for four synthetic data
centers. Here we use proximal policy optimization (PPO) to choose where
service runs and when and where batch work runs. We ask whether this
scheduler can reduce regional net-load ramps and monthly peaks while
meeting service, completion-window, and site-capacity constraints.

Previous work uses workload flexibility to change when data centers draw
power. Google's Carbon-Aware Computing system shifts flexible work using
forecasts and daily completion requirements \cite{radovanovic2021carbonaware}.
DeepEE applies deep reinforcement learning to joint job scheduling and
cooling control \cite{ran2019deepee}. SustainDC provides a benchmark for
workload, cooling, and battery control using observations, forecasts, and
separate operating metrics \cite{naug2024sustaindc}. These studies inform our
use of workload flexibility and learned control. Our focus is their effect
on regional net-load ramps and peaks; we do not measure carbon savings.

We evaluate three independently trained policies on May 2025, using the
same no-flexibility baseline, \(\mu_{\mathrm{NF}}\), which executes all work
at its origin in its arrival hour. The study's contribution is the
scheduling model and its evaluation using Google workload traces and EIA
grid data. PPO supplies the learning method. Appendix~\ref{app:implementation}
gives the code locations and reproduction commands.

\section{The Scheduling Method}
\label{sec:scheduler}

One interaction is one decision hour. The scheduler observes the available
grid information and workload, chooses an action, and receives a reward
once the hour's realized net load is known. One episode is a calendar month
of 672 to 744 hours. Queues and site-power history carry across midnight;
each month ends with empty queues.

\subsection{Observation}
\label{subsec:observation}

The policy receives an 82-number observation built from the information
available at hour \(t\). For each market, it includes gross demand and net
load from \(t-1\), the four gross-demand and four net-load forecasts issued
at \(t-1\), the steepest forecast climb for each quantity, and the running
adjusted and original peaks. For each site, it includes current service and
batch arrivals, queued batch work, and the previous hour's power. Five
queue-urgency totals, cyclic hour and weekday features, and the fraction of
the month remaining complete the observation.

Current arrivals are known when scheduling; realized grid measurements are
available only through \(t-1\). The observation summarizes the available
information rather than exposing the simulator's full state.

\subsection{Actions and feasibility}
\label{subsec:actions}

PPO outputs nine preferences in \([-6,6]\): four for service destinations,
one for batch release, and four for batch destinations. A decoder turns
these preferences into the executed schedule:
\begin{itemize}
  \item A softmax splits service among sites, and a projection keeps the
        allocation within each site's capacity.
  \item The release preference maps to a fraction of queued batch work.
        The decoder increases release when needed to meet deadlines and
        limits it to the remaining capacity.
  \item A softmax splits released batch work among destinations, subject to
        capacity. A global earliest-deadline-first queue selects the work.
\end{itemize}
PPO learns scheduling preferences; the decoder enforces service, deadline,
and capacity constraints. Feasibility therefore comes from the decoder,
without constraint penalties in the reward.

\subsection{Reward}
\label{subsec:reward}

The raw hourly reward follows the form of the joint objective from the
problem statement:
\begin{equation}
  \begin{aligned}
    r_t
    ={}&-\frac{0.5}{C_R}\sum_m I_{m,t}\\
    &-\frac{0.5}{C_Q}\sum_m\left[
      \left(M_{m,t}-M_{m,t-1}\right)
      -\left(O_{m,t}-O_{m,t-1}\right)
    \right],
  \end{aligned}
  \label{eq:reward}
\end{equation}
where \(I_{m,t}\) is ramp impact, and \(M_{m,t}\) and \(O_{m,t}\) are the
running normalized adjusted and original peaks. Each maximum is initialized
from the first decision hour; its first increment is measured from zero.
The preceding warm hour contributes to the first ramp but not to the peaks.

The peak increments sum to the month's peak impact, so the raw,
undiscounted episode return is \(\sum_t r_t=-J_e\). Lowering the joint score
therefore increases this return. Negative ramp impacts and positive hourly
rewards are valid.

Both reference scales use total-grid quantities under no flexibility,
as defined in the problem statement.

\section{Training with PPO}
\label{sec:ppo}

PPO handles the decoder's continuous preference actions. We use its
Stable-Baselines3 implementation \cite{schulman2017ppo,raffin2021sb3} with an
actor--critic network: the policy chooses actions, while a separate value
function estimates discounted training returns
\cite{sutton2018reinforcement}.

\subsection{Policy updates}

Each rollout collects 512 steps from each of four environments, or 2,048
transitions. The policy samples actions from a diagonal Gaussian with a
learned mean and per-dimension standard deviations. Samples are clipped to
\([-6,6]\) before reaching the decoder.

Generalized advantage estimation \cite{schulman2016gae} uses the rewards
and value estimates to assess which sampled actions did better than
expected. Estimates stop at the end of the rollout or month. At an
unfinished rollout, the value function estimates the remaining return;
at month-end, there is no future return. PPO then updates the policy using
its clipped objective. Clipping limits the incentive for large policy
changes; it does not impose a hard 20\% bound on action-probability changes.
Appendix~\ref{subsec:ppo-details} gives the update equations.

\subsection{Normalization and discounting}

During training, observations are standardized using running means and
variances and clipped to \(\pm10\). Rewards are divided by a running
standard deviation of discounted returns and clipped to \(\pm10\).
The policy and value function train on these transformed values; reported
rewards and \(J_e\) use raw values.

PPO uses \(\gamma=0.99\), so training favors earlier rewards. It is an
approximate method for the monthly problem: evaluation uses the raw,
undiscounted objective, while training uses discounted, normalized rewards.

\subsection{Training setup}

The policy and value networks each have two hidden layers of 64 tanh units,
with 19,603 trainable parameters in total. Table~\ref{tab:hyperparameters}
lists the pilot settings. We have not tuned them for this problem.

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{PPO settings used in the pilot.}
  \label{tab:hyperparameters}
  \begin{tabular}{@{}lr@{}}
    \toprule
    Setting & Value \\
    \midrule
    Parallel environments & 4 \\
    Steps per environment per rollout & 512 \\
    Transitions per update & 2,048 \\
    Epochs per update & 10 \\
    Minibatch size & 256 \\
    Learning rate (Adam) & \(3\times10^{-4}\) \\
    Discount \(\gamma\) & 0.99 \\
    GAE \(\lambda\) & 0.95 \\
    Clip range \(\epsilon\) & 0.2 \\
    Value-loss weight & 0.5 \\
    Entropy bonus & 0 \\
    Gradient-norm limit & 0.5 \\
    Hidden layers (policy, value) & 64, 64 (tanh) \\
    Initial action standard deviation & 1 \\
    Interactions per run & 1,024,000 \\
    \bottomrule
  \end{tabular}
\end{table}

Each seed trains a fresh policy on the eleven training months of 2025,
excluding May. Each environment cycles through a shuffled order of these
months before repeating them. The requested 1,000,000 interactions are
rounded up to a checkpoint boundary, giving 1,024,000 interactions per seed.

The pilot uses seeds 4101--4103. A seed controls network initialization,
action sampling, minibatch order, and the order of training months. Data,
constraints, and hyperparameters stay the same, so differences between
seeds measure variation from training randomness.

\section{Evaluation}
\label{sec:evaluation}

Each saved policy runs on May 2025, one continuous 744-hour episode. It acts
deterministically, using the Gaussian mean instead of sampling. Observation
normalization uses frozen training statistics, and rewards are reported
without normalization. The no-flexibility baseline runs on the same hours,
with the same arrivals, grid data, and forecasts.

We report \(J_e(\mu_{\mathrm{NF}})-J_e(\mu)\), positive when PPO improves on
the baseline, and show the ramp and peak contributions separately. Regional
results report squared-ramp reductions relative to the original grid and
peak changes in MW relative to the no-flexibility fleet. These references
are labeled separately in the figures and tables.

We also report percentage reductions from no flexibility in total squared
normalized ramps, summed normalized regional peaks, and the fleet's added
peak impact. Each percentage divides the reduction by the corresponding
no-flexibility value. The signed joint score is reported as an absolute
improvement.

For each final checkpoint, we also recomputed \(J_e\) from the raw grid
panel and site-power trajectory. It matched the negative raw episode return
within numerical tolerance. All three policies completed the evaluation
with no service, deadline, or capacity violation.

May informed the choice of forecast lead times and is used to inspect
checkpoint performance. It is a validation month, not an untouched test
set. The pilot assesses three trained policies on this one month.

\section{Pilot Results}
\label{sec:results}

\subsection{Training}

Fig.~\ref{fig:learning} shows the return of each completed training month.
All three seeds begin below the average no-flexibility return and cross
that level within about 50,000
interactions. Returns rise quickly to about 0.010 by 400,000 interactions and
then climb slowly to 0.013--0.014. The three curves follow similar trends.

\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{learning_curves.pdf}
  \caption{Return of each completed training month (\(-J_e\), higher is
  better), as a 10-rollout rolling mean. The gray line is the no-flexibility
  baseline's return averaged over the eleven training months,
  \(-0.00544\).}
  \label{fig:learning}
\end{figure}

\subsection{May improvement}

Table~\ref{tab:may} and Fig.~\ref{fig:checkpoints} give the May results. The
baseline scores \(J_e=0.005613\). Every checkpoint of every seed improves on
it, and the final policies reach \(J_e\) between \(-0.0097\) and
\(-0.0114\), improvements of 0.0153 to 0.0170. A negative joint score means
that the scaled ramp reduction relative to the original grid outweighs the
fleet's added peak impact.

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{May improvement \(J_e(\mu_{\mathrm{NF}})-J_e(\mu)\) by checkpoint,
  and the split of the final improvement into its weighted ramp and peak
  parts.}
  \label{tab:may}
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    & Seed 4101 & Seed 4102 & Seed 4103 \\
    \midrule
    153,600 interactions   & 0.0131 & 0.0125 & 0.0134 \\
    512,000 interactions   & 0.0155 & 0.0155 & 0.0162 \\
    1,024,000 interactions & 0.0153 & 0.0165 & 0.0170 \\
    \midrule
    Final \(J_e(\mu)\)     & \(-0.0097\) & \(-0.0109\) & \(-0.0114\) \\
    Ramp part              & 0.0139 & 0.0152 & 0.0155 \\
    Peak part              & 0.0014 & 0.0013 & 0.0016 \\
    \bottomrule
  \end{tabular}
\end{table}

\begin{figure}[!t]
  \centering
  \includegraphics[width=\columnwidth]{checkpoint_improvement.pdf}
  \caption{May improvement over the no-flexibility baseline at each saved
  checkpoint.}
  \label{fig:checkpoints}
\end{figure}

Relative to no flexibility, the final policies reduce total squared
normalized ramps by 2.8--3.1\% and the sum of normalized regional peaks by
0.29--0.36\% (Table~\ref{tab:nf-percentages}). That peak reduction removes
23--28\% of the fleet's added peak impact, \(\sum_m\Phi_{m,e}\), which falls
from 0.0411 to 0.0296--0.0316.

\begin{table}[htbp]
  \centering
  \footnotesize
  \caption{May reductions from no flexibility (\%) for the final policies.
  Peaks are summed after regional normalization.}
  \label{tab:nf-percentages}
  \begin{tabular}{@{}lrrr@{}}
    \toprule
    Quantity & 4101 & 4102 & 4103 \\
    \midrule
    Total squared ramps & 2.83 & 3.08 & 3.14 \\
    Total regional peaks & 0.31 & 0.29 & 0.36 \\
    Fleet-added peak impact & 24.37 & 22.98 & 27.89 \\
    \bottomrule
  \end{tabular}
\end{table}

About 90\% of the joint improvement comes from the ramp component. The ramp
gain is mostly in place by 153,600 interactions, while the peak gain roughly
doubles between the first and last checkpoints.

\subsection{Regions}

Fig.~\ref{fig:regions} and Table~\ref{tab:regions} break the result down by
region. ISO-NE NEMA, where a site's power range is largest relative to typical
ramps, gains most: its squared-ramp total is 11--14\% below the original grid's,
and its monthly peak is 109--140 MW below the no-flexibility baseline.
CAISO NP15 and SPP North improve on both measures. MISO Minnesota, where a
site's power is smallest relative to the grid, changes
little, and its peak rises by up to 35 MW in two seeds.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=\textwidth]{regional_results.pdf}
  \caption{May results by region for the final policies. Left: percentage
  cut in the total squared one-hour ramp relative to the original grid. Right:
  change in the monthly adjusted net-load peak relative to the no-flexibility
  baseline; negative is a reduction.}
  \label{fig:regions}
\end{figure*}

\begin{table*}[htbp]
  \centering
  \footnotesize
  \caption{Regional May results for the final policies.}
  \label{tab:regions}
  \begin{tabular}{@{}lrrrrrr@{}}
    \toprule
    & \multicolumn{3}{c}{Squared-ramp cut vs.\ original (\%)}
    & \multicolumn{3}{c}{Peak change vs.\ baseline (MW)} \\
    \cmidrule(lr){2-4}\cmidrule(l){5-7}
    Market case & 4101 & 4102 & 4103 & 4101 & 4102 & 4103 \\
    \midrule
    CAISO NP15     & 2.0  & 2.1  & 1.9  & \(-88\)  & \(-78\)  & \(-91\) \\
    MISO Minnesota & 0.0  & \(-0.4\) & 0.0 & \(-37\) & \(+16\) & \(+35\) \\
    SPP North      & 0.7  & 1.2  & 0.9  & \(-46\)  & \(-58\)  & \(-62\) \\
    ISO-NE NEMA    & 11.0 & 12.2 & 14.1 & \(-109\) & \(-109\) & \(-140\) \\
    \bottomrule
  \end{tabular}
\end{table*}

\subsection{How the policies act}

The policies mainly reallocate service between sites. The allocation
diagnostic indicates that about 31\% of May's service work changes sites,
while batch work waits 1.0--1.5 hours on average within its 24-hour window.
The decoder adjusts the requested schedule in 68--75\% of hours to meet
capacity and deadline requirements.

Fig.~\ref{fig:isone} shows ISO-NE over three days around its May peak hour.
Under the baseline, site power stays near 340 MW. The PPO policy instead runs
the site near full power while net load is low and cuts it to about 200 MW as
net load climbs to its evening peak. At the peak hour it draws 231 MW, 109 MW
less than the baseline.

\begin{figure*}[!t]
  \centering
  \includegraphics[width=\textwidth]{isone_peak_window.pdf}
  \caption{ISO-NE NEMA from 15 to 18 May 2025. Top: original net load. Bottom:
  site power under the no-flexibility baseline and the seed-4102 policy. The
  vertical line marks the baseline's monthly peak hour, 16 May 23:00 UTC.}
  \label{fig:isone}
\end{figure*}

\section{Limitations and Next Steps}
\label{sec:limitations}

The three policies improve on no-flexibility in the May simulation. This
is preliminary evidence from one validation month. The planned campaign
uses seeds 4101--4110 to assess training variation more closely; additional
seeds do not add new grid or workload conditions.

The next comparison is a simple forecast-aware rule using the same
observations, workload constraints, and feasibility decoder. That comparison
would help determine what learning adds beyond flexible scheduling. No
heuristic results are included in this pilot.

\begin{samepage}
We have not yet compared ramp-only and peak-only policies. Those runs would
help separate the objectives' effects. Routing remains free and
instantaneous, making the available geographic flexibility optimistic.

\end{samepage}

\clearpage
\appendices
\onecolumn
\section{Implementation Notes}
\label{app:implementation}

Paths are relative to the repository root. These notes connect the methods
and results to the implementation and give the commands used for the pilot.
The problem-statement appendix documents workload construction, grid data,
and the power model.

\subsection{Environment, observations, and actions
  (Section~\ref{sec:scheduler})}
\begin{itemize}
  \item \codepath{RampAwareEnv} in \codepath{env/ramp_v6/environment.py}
        implements the hourly simulation. \codepath{make_four_market_env}
        in \codepath{energy_model_v3/four_market_v2.py} builds it from the
        generated monthly factory.
  \item \codepath{_build_observation_schema} and \codepath{_observation}
        in \codepath{environment.py} define and fill the 82 observation
        entries. The simulator retains the full queues; the policy receives
        queue totals and urgency summaries.
  \item \codepath{action_schema} names the nine preferences.
        \codepath{project_action} in \codepath{env/ramp_v6/projection.py}
        converts them to service allocations, batch releases, and batch
        destinations. The environment drains the global queue in
        earliest-deadline-first order.
  \item \codepath{JointObjective.reward_components} in
        \codepath{env/ramp_v6/objective.py} computes
        Equation~\eqref{eq:reward}. The environment maintains running
        adjusted and original peaks. The first decision initializes each
        maximum, with zero as the reference for its first increment.
  \item \codepath{tests/ramp_v6/test_joint_objective.py} checks the raw
        reward--objective identity. \codepath{tests/ramp_v6/test_continuous_month.py}
        checks chronology and queue behavior, and
        \codepath{tests/energy_model_v3/test_monthly_ramp_impact.py} checks
        the problem statement's worked example.
\end{itemize}

\subsection{PPO update equations}
\label{subsec:ppo-details}

The policy \(\pi_\theta(a\mid o)\) describes the distribution of sampled actions;
\(V_\phi(o)\) estimates discounted training returns. Here \(o_t\) is the
normalized observation and \(\widetilde r_t\) is the normalized and clipped
training reward. Generalized advantage estimation uses
\begin{align}
  \delta_t
  &=\widetilde r_t+\gamma V_\phi(o_{t+1})-V_\phi(o_t),
  \label{eq:td-error}\\
  \hat A_t
  &=\sum_{l=0}^{K_t-1}(\gamma\lambda)^l\delta_{t+l},
  \label{eq:gae}
\end{align}
where \(K_t\) is the number of collected steps from \(t\) to the earlier of
the rollout boundary or month-end. At month-end the next-state value is
zero. At an unfinished rollout boundary it supplies the bootstrap value.
The pilot uses \(\gamma=0.99\) and \(\lambda=0.95\).

Advantages are standardized within each minibatch before the policy update.
For the sampled action \(a_t\), define
\(\rho_t(\theta)=\pi_\theta(a_t\mid o_t)/
\pi_{\theta_{\mathrm{old}}}(a_t\mid o_t)\). PPO maximizes
\begin{equation}
  L^{\mathrm{clip}}(\theta)
  =\mathbb E_t\!\left[
    \min\!\left(
      \rho_t\hat A_t,\;
      \mathrm{clip}(\rho_t,1-\epsilon,1+\epsilon)\hat A_t
    \right)
  \right],
  \label{eq:ppo-clip}
\end{equation}
with \(\epsilon=0.2\). The probability ratio uses the sampled Gaussian
action; the environment receives its clipped version. Clipping limits the
incentive for large policy changes; it does not enforce a bound on the
policy itself.

The value network fits estimated returns with squared error, weighted 0.5
in the combined loss. There is no entropy bonus, and gradients are clipped
to norm 0.5. These settings are listed in Table~\ref{tab:hyperparameters}.

\subsection{Training and checkpoints (Section~\ref{sec:ppo})}
\begin{itemize}
  \item The saved pilot uses \(C_R=6.034057752312674\) and
        \(C_Q=3.664598536383508\), matching the problem statement's total-grid
        references. Both are means over the eleven training months under
        no flexibility: total squared adjusted ramps and summed normalized
        adjusted peaks, respectively.
  \item \codepath{run_training} in \codepath{ramp_rl/runner.py} reads the
        PPO settings from \codepath{env/protocols/four_market_v2.yaml}.
        Stable-Baselines3 2.9.0 is pinned in \codepath{requirements.txt},
        and the runner checks the installed version.
  \item \codepath{VecNormalize} standardizes and clips observations and
        scales and clips training rewards. Its statistics are saved in
        \codepath{vecnormalize.pkl}. Evaluation freezes those statistics
        and disables reward normalization.
  \item Four environment copies step in turn within one training process.
        Each rollout provides 2,048 transitions. Ten epochs with minibatches
        of 256 give 80 gradient steps per update; the pilot's 1,024,000
        interactions give 500 updates.
  \item \codepath{FourMarketV2WindowEnv._next_window_id} in
        \codepath{energy_model_v3/four_market_v2.py} draws the eleven training
        months in shuffled cycles. The runner seeds Python, NumPy, PyTorch,
        PPO, and the environment copies to control training randomness.
  \item \codepath{TrainingCallback} writes raw metrics to
        \codepath{learning_curve.csv} after each rollout.
        \codepath{save_latest_checkpoint} saves a resumable checkpoint every
        25 rollouts, or 51,200 interactions. The pilot evaluates milestones
        at 153,600, 512,000, and 1,024,000 interactions.
  \item \codepath{environment_identity} in \codepath{ramp_rl/contract.py}
        records the objective, input digest, and observation and action
        shapes. Resume checks also compare PPO settings and seed. The
        evaluator rejects checkpoints with incompatible environment
        identities. \codepath{tests/ramp_v6/test_campaign_instrumentation.py}
        tests these checks and checkpoint resumption.
\end{itemize}
On the eight-core laptop used for the pilot, each seed trained at about
240 interactions per second and finished in 71--72 minutes, with all three
seeds running concurrently.

\subsection{Evaluation and figures
  (Sections~\ref{sec:evaluation}--\ref{sec:results})}
\begin{itemize}
  \item \codepath{evaluate_checkpoint} and \codepath{_run_episode} in
        \codepath{ramp_rl/evaluation.py} run the deterministic policy and
        no-flexibility baseline. They report raw joint scores, component
        outcomes, regional metrics, and feasibility checks.
  \item \codepath{scripts/run_four_market_v2_pilot.py} trains the selected
        seeds and evaluates saved milestones. The pilot's summaries and
        learning curves are under
        \codepath{output/four_market_joint_v3/pilot/impact-50-50-1m/};
        checkpoints are under
        \codepath{models/four_market_joint_v3/pilot/impact-50-50-1m/}.
  \item \codepath{scripts/build_ppo_pilot_figures.py} draws the figures from
        those outputs and writes their supporting values to
        \codepath{docs/figures/ppo_pilot/figure_data.json}. Squared-ramp
        percentages in the regional figure use the original grid's
        squared-ramp total. Table~\ref{tab:nf-percentages} instead compares
        each final policy with no flexibility; its values are stored under
        \codepath{no_flexibility_percentage_reductions}. The ramp denominator
        is the baseline's total squared adjusted ramps, the peak denominator
        is its sum of normalized adjusted regional peaks, and the added-peak
        denominator is \(\sum_m\Phi_{m,e}(\mu_{\mathrm{NF}})\). All three are
        positive for this month.
  \item The service-reallocation diagnostic uses differences between
        destination allocations and origin arrivals. It measures the
        minimum work that must have changed sites, rather than tracking
        individual service jobs.
\end{itemize}

\subsection{Reproduction commands}
From the repository root, run the following commands in PowerShell:
{\small
\begin{verbatim}
python scripts/build_four_market_v2_factory.py
python scripts/run_four_market_v2_pilot.py `
    --seeds 4101 4102 4103 `
    --timesteps 1000000 --tag impact-50-50-1m
python scripts/build_ppo_pilot_figures.py `
    --tag impact-50-50-1m
\end{verbatim}
}
The pilot script runs seeds in separate processes. The option
\codepath{--ramp-weight} selects ramp-only (1), equal-weight (0.5), or
peak-only (0) objectives. The planned ten-seed campaign is run by
\codepath{scripts/run_four_market_v2_campaign.py}.

\begin{thebibliography}{99}

\bibitem{radovanovic2021carbonaware}
A. Radovanovi\'{c} \emph{et al.}, ``Carbon-Aware Computing for Datacenters,''
arXiv:2106.11750v1, 2021. [Online]. Available:
\url{https://arxiv.org/abs/2106.11750v1}.

\bibitem{ran2019deepee}
Y. Ran, H. Hu, X. Zhou, and Y. Wen, ``DeepEE: Joint optimization of job
scheduling and cooling control for data center energy efficiency using deep
reinforcement learning,'' in \emph{Proc. IEEE Int. Conf. Distributed
Computing Systems (ICDCS)}, 2019, pp. 645--655,
doi: 10.1109/ICDCS.2019.00070.

\bibitem{naug2024sustaindc}
A. Naug \emph{et al.}, ``SustainDC: Benchmarking for sustainable data center
control,'' in \emph{Advances in Neural Information Processing Systems},
vol. 37, Datasets and Benchmarks Track, 2024,
doi: 10.52202/079017-3192.

\bibitem{sutton2018reinforcement}
R. S. Sutton and A. G. Barto, \emph{Reinforcement Learning: An Introduction},
2nd ed. Cambridge, MA, USA: MIT Press, 2018.

\bibitem{schulman2017ppo}
J. Schulman, F. Wolski, P. Dhariwal, A. Radford, and O. Klimov, ``Proximal
policy optimization algorithms,'' arXiv:1707.06347, 2017.

\bibitem{schulman2016gae}
J. Schulman, P. Moritz, S. Levine, M. Jordan, and P. Abbeel,
``High-dimensional continuous control using generalized advantage
estimation,'' in \emph{Proc. Int. Conf. Learn. Represent. (ICLR)}, 2016.

\bibitem{raffin2021sb3}
A. Raffin, A. Hill, A. Gleave, A. Kanervisto, M. Ernestus, and N. Dormann,
``Stable-Baselines3: Reliable reinforcement learning implementations,''
\emph{J. Mach. Learn. Res.}, vol. 22, no. 268, pp. 1--8, 2021.

\end{thebibliography}

\end{document}
